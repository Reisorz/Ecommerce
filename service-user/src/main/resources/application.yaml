spring:
  application:
    name: service-user
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://keycloak:8080/realms/ecommerce-dev/protocol/openid-connect/certs
server:
  port: 8082